name: Deploy Streamlit to Streamlit Cloud

# Streamlit Cloud은 GitHub 저장소와 직접 연동됩니다.
# 이 워크플로우는 코드 품질 검사용입니다.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Check app syntax
        run: python -m py_compile app.py && echo "✅ app.py 문법 검사 통과"
